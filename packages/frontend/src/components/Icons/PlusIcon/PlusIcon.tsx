import { PlusIcon as PlusIconAksel } from '@navikt/aksel-icons';
import styles from './plusIcon.module.css';
export const PlusIcon = () => {
  return <PlusIconAksel fontSize="1.25rem" className={styles.plusIcon} />;
};
